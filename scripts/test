#!/usr/bin/env bash
# Run the test suite in the Python 3.13 environment.

set -e

cd "$(dirname "$0")/.."

if [[ ! -x ".venv/bin/pytest" ]]; then
    echo "Test environment not found — running scripts/setup..."
    scripts/setup
fi

.venv/bin/pytest "$@"
