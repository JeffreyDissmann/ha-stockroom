#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

if [[ -f ".venv/bin/activate" ]]; then
    # shellcheck disable=SC1091
    source .venv/bin/activate
fi

ruff check custom_components/stockroom tests
ruff format --check custom_components/stockroom tests
